% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stops.R
\name{tm_stop_id}
\alias{tm_stop_id}
\title{Look up numeric stop IDs for a rapid transit station}
\usage{
tm_stop_id(line, stop_name, direction)
}
\arguments{
\item{line}{Line name: \code{"Red"}, \code{"Orange"}, \code{"Blue"}, \code{"Green"}, or
\code{"Mattapan"} (case-insensitive; \code{"line-red"} form also accepted).}

\item{stop_name}{Station name (case-insensitive), e.g. \code{"Alewife"},
\code{"Davis"}. Use \code{\link[=tm_line_stations]{tm_line_stations()}} to see all valid names.}

\item{direction}{Direction of travel. Either the direction name for the line
(e.g. \code{"northbound"}, \code{"southbound"}, \code{"eastbound"}, \code{"westbound"}) or the
numeric code \code{0} or \code{1}. Use \code{\link[=tm_line_directions]{tm_line_directions()}} to see which names
apply to a given line.}
}
\value{
A character vector of stop IDs (most stations have one; branching
or multi-platform stations may have several).
}
\description{
Returns the stop ID(s) used by the headway, dwell, and travel-time endpoints
for a given station and direction of travel.
}
\examples{
tm_stop_id("Red", "Alewife", "southbound")
tm_stop_id("Red", "Davis", 0)
tm_stop_id("Green", "Park Street", "eastbound")
}
